{
  "build": {
    "builder": "NIXPACKS",
    "config": {
      "phases": {
        "install": {
          "cmds": ["pip install --upgrade pip"]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "uvicorn app:app --host 0.0.0.0 --port $PORT"
  }
}
